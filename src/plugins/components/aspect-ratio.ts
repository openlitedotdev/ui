import { cva } from 'class-variance-authority'

/**
 * Aspect Ratio wrapper **Class Variants** component
 *
 * const styles = aspectRatio({})
 *
 * @example
 * <AspectRatio role="img" className={styles())}>
 *   // alert elements
 * </AspectRatio>
 */
export const aspectRatio = cva('')
